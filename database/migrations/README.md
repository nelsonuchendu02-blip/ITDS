# Database migrations

The migrations directory is reserved for PostgreSQL schema changes in future phases.

This Phase 0 release prepares the directory structure and documentation only. No application tables are created yet.
